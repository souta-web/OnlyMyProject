import 'package:flutter/material.dart';

class ConfigScreenWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Config'),
      ),
      body: Center(
        child: Text('Config Screen'),
      ),
    );
  }
}